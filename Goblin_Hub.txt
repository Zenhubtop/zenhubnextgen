function scriptLoad()
local coreGui = game:GetService("CoreGui")
local tweenService = game:GetService("TweenService");

function createTween(Object,setup,settings)
local style = (settings.Style or Enum.EasingStyle.Sine);
local direction = (settings.Direction or Enum.EasingDirection.InOut);
local length = (settings.TIME or 1);
local repeatcount = (settings.repeatLimit or 0);
local reverse = (settings.IsReversed or false);
local dt = (settings.delay or 0);

local tween = TweenInfo.new(
  length, style, direction, repeatcount, reverse, dt
);

local tweenService = game:GetService("TweenService");
local Anim = tweenService:Create(Object,tween,setup);
Anim:Play();
return Anim;
end;

spawn(function()
  local BlueXLoader = Instance.new("ScreenGui")
  local Loader = Instance.new("Frame")
  local UICorner = Instance.new("UICorner")
  local Menu = Instance.new("Frame")
  local UICorner_2 = Instance.new("UICorner")
  local Title = Instance.new("TextLabel")
  local LoadBar = Instance.new("Frame")
  local UICorner_3 = Instance.new("UICorner")
  local Bar = Instance.new("Frame")
  local UICorner_4 = Instance.new("UICorner")
  local Message = Instance.new("TextLabel")
  local MainImage = Instance.new("ImageLabel")
  local MainImageFrame = Instance.new("Frame")

  BlueXLoader.Name = "BlueXLoader"
  BlueXLoader.Parent = coreGui
  BlueXLoader.ResetOnSpawn = false

  Loader.Name = "Loader"
  Loader.Parent = BlueXLoader
  Loader.AnchorPoint = Vector2.new(0.5, 0.5)
  Loader.BackgroundColor3 = Color3.new(64, 64, 64)
  Loader.BackgroundTransparency = 0.6
  Loader.BorderColor3 = Color3.fromRGB(64,64,64)
  Loader.BorderSizePixel = 2
  Loader.Position = UDim2.new(0.5, 0, 0.5, 0)
  Loader.Size = UDim2.new(0, 0, 0, 0)

  UICorner.CornerRadius = UDim.new(0, 6)
  UICorner.Parent = Loader

  Menu.Name = "Menu"
  Menu.Parent = Loader
  Menu.AnchorPoint = Vector2.new(0.5, 0.5)
  Menu.BackgroundColor3 = Color3.new(0.0680392, 0.0680392, 0.0680392)
  Menu.Position = UDim2.new(0.5, 0, 0.5, 0)
  Menu.Size = UDim2.new(1, -4, 1, -4)

  UICorner_2.CornerRadius = UDim.new(0, 6)
  UICorner_2.Parent = Menu

  Title.Name = "Title"
  Title.Parent = Menu
  Title.AnchorPoint = Vector2.new(0.5, 0)
  Title.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
  Title.BackgroundTransparency = 1.000
  Title.BorderSizePixel = 0
  Title.Position = UDim2.new(0.647, 0, 0, 20)
  Title.Size = UDim2.new(1, -6, -0.262604743, 55)
  Title.ZIndex = 4
  Title.Font = Enum.Font.GothamBold
  Title.Text = "Welcome To ".."<font color='rgb(178, 102, 255)'>ZEN HUB</font>"
  Title.RichText = true;
  Title.TextColor3 = Color3.fromRGB(190, 190, 190)
  Title.TextSize = 23.500
  Title.TextTransparency = 1

  MainImage.Name = "MainImage"
  MainImage.Parent = Loader
  MainImage.BackgroundTransparency = 1.000
  MainImage.Position = UDim2.new(0, -10, 0, -5)
  MainImage.Size = UDim2.new(0, 150, 0, 110)
  MainImage.Image = "rbxassetid://14103696428"
  MainImage.ImageTransparency = 1.000

  LoadBar.Name = "LoadBar"
  LoadBar.Parent = Menu
  LoadBar.AnchorPoint = Vector2.new(0.5, 0)
  LoadBar.BackgroundColor3 = Color3.fromRGB(49, 49, 49)
  LoadBar.BorderSizePixel = 0
  LoadBar.Position = UDim2.new(0.5, 0, 1, -28)
  LoadBar.Size = UDim2.new(1, -28, 0, 20)
  LoadBar.BackgroundTransparency = 1

  UICorner_3.CornerRadius = UDim.new(0, 4)
  UICorner_3.Parent = LoadBar

  Bar.Name = "Bar"
  Bar.Parent = LoadBar
  Bar.BackgroundColor3 = Color3.fromRGB(190,190,190)
  Bar.BorderSizePixel = 0
  Bar.Size = UDim2.new(0, 0, 1, 0)
  Bar.ZIndex = 4
  Bar.BackgroundTransparency = 1

  UICorner_4.CornerRadius = UDim.new(0, 4)
  UICorner_4.Parent = Bar

  Message.Name = "Message"
  Message.Parent = Menu
  Message.AnchorPoint = Vector2.new(0.5, 0)
  Message.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
  Message.BackgroundTransparency = 1.000
  Message.BorderSizePixel = 0
  Message.Position = UDim2.new(0.6, 0, 0, 45)
  Message.Size = UDim2.new(1, -28, 0, 28)
  Message.SizeConstraint = Enum.SizeConstraint.RelativeXX
  Message.ZIndex = 4
  Message.Font = Enum.Font.SourceSansSemibold
  Message.Text = "Welcome, " .. game:GetService("Players").LocalPlayer.Name --settings.StartMessage or "Welcome, " .. game:GetService("Players").LocalPlayer.Name
  Message.TextColor3 = Color3.fromRGB(197, 197, 197)
  Message.TextSize = 17.000
  Message.TextTransparency = 1

  wait(2)

  createTween(Loader, {
    Size = UDim2.new(0, 400, 0, 120)}, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })

  createTween(Title, {
    TextTransparency = 0
  }, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })

  createTween(MainImage, {
    ImageTransparency = 0
  }, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })

  createTween(Message, {
    TextTransparency = 0
  }, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  }).Completed:Wait(1)


  createTween(LoadBar, {
    BackgroundTransparency = 0
  }, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })

  createTween(Bar, {
    BackgroundTransparency = 0
  }, {
    Style = Enum.EasingStyle.Cubic,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })

  function setSize(int)
  local number = tonumber(int)
  local percent = 1 - ((100 - int) / (100 - 0));

  createTween(Bar, {
    Size = UDim2.new(percent, 0, 1, 0)}, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.In,
    TIME = .4
  })
  end

  wait(1)
  setSize(20)

  for i = 1, 3 do
  Message.Text = "Checking ."
  wait(.4)
  Message.Text = "Checking . ."
  wait(.4)
  Message.Text = "Checking . . ."
  wait(.4)
  end
  setSize(60)
  Message.Text = "Game Found!"
  wait(0.5)
  setSize(80)
  Message.Text = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
  wait(0.5)
  setSize(100)

  createTween(Bar, {
    BackgroundTransparency = 1
  }, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  }).Completed:Wait()

  createTween(LoadBar, {
    BackgroundTransparency = 1
  }, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  })

  Message.Text = "Loaded | Enjoy!"
  wait(.5)
  createTween(Message, {
    TextTransparency = 1
  }, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  })

  createTween(Title, {
    TextTransparency = 1
  }, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  })

  createTween(MainImage, {
    ImageTransparency = 1
  }, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  })

  createTween(Loader, {
    Size = UDim2.new(0, 0, 0, 0)}, {
    Style = Enum.EasingStyle.Quart,
    Direction = Enum.EasingDirection.Out,
    TIME = .4
  }).Completed:Wait(1)
  BlueXLoader:Destroy()
  if game.PlaceId == 2753915549 then
loadstring(game:HttpGet("https://raw.githubusercontent.com/ZenHubTheBest/Main/main/gameopalskdbbdjskspwlwkksobdshsiownwhshpwpwnnsnoseoemndnd",true))()
elseif game.PlaceId == 4442272183 then
loadstring(game:HttpGet("https://raw.githubusercontent.com/ZenHubTheBest/Main/main/gameopalskdbbdjskspwlwkksobdshsiownwhshpwpwnnsnoseoemndnd",true))()
elseif game.PlaceId == 7449423635 then
loadstring(game:HttpGet("https://raw.githubusercontent.com/ZenHubTheBest/Main/main/gameopalskdbbdjskspwlwkksobdshsiownwhshpwpwnnsnoseoemndnd",true))()
elseif game.PlaceId == 2788229376 then
loadstring(game:HttpGet("https://sharetext.me/raw/83mf11f4jf", true))()
elseif game.PlaceId == 286090429 then
loadstring(game:HttpGet("https://sharetext.me/raw/exbfgydpbn", true))()
elseif game.PlaceId == 6381829480 or game.PlaceId == 5931540094 or
game.PlaceId == 4520749081 then
loadstring(game:HttpGet(
  "https://raw.githubusercontent.com/ZenHubTheBest/Main/main/kinggagcwcqgindospspsb",
  true))()
end
end)
end

-- Key System UI
local KeySystemUI = {
    Closed = false
}
local UIMade = false;
-- PandaAuth Initialization
local ServiceName = "zenhub"
local AntiBypass = true
local PandaAuth = loadstring(game:HttpGet('https://pandadevelopment.net/servicelib?service='..ServiceName..'&core=roblox&param=v3'))()
PandaAuth:Set({
    Service = ServiceName,
    APIToken = "PandaDevelopmentGG", -- Make sure to sync with the API Token you have on your Dashboard's Hub Setting
    TrueEndpoint = "authenticated", -- If the Value is Corrected, it returns as this value
    FalseEndpoint = "not_auth", -- If the Value is Corrected, it returns as this value
    Debug = false -- Unless you want to Debug the Key System
})

local True = PandaAuth:Get().Internal.TrueEndpoint 
local False = PandaAuth:Get().Internal.FalseEndpoint

-- Validation Key Function
local function ValidateKey(Key)
    local result = PandaAuth:ValidateKey(Key)
    return result;
end

_G.Color = Color3.fromRGB(60, 60, 60)
local TWS = game:GetService("TweenService")
-- Services
local UserInputService = game:GetService("UserInputService")
local CoreGUI = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local httprequest = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request or HttpPost

-- Hide UI
local HIDEUI = get_hidden_gui or gethui

if syn and typeof(syn) == "table" and RenderWindow then
    syn.protect_gui = gethui;
end

local RemoveStringsInvite = {
    "discord.gg", "discord.com/invite"
}

-- Discord Invite Function
local function JoinDiscord(DiscordInvite)
    if setclipboard then
        setclipboard(DiscordInvite)
    end

    if httprequest then
        for _, v in pairs(RemoveStringsInvite) do
            DiscordInvite = string.gsub(DiscordInvite, "https://", "")
            DiscordInvite = string.gsub(DiscordInvite, "http://", "")
            DiscordInvite = string.gsub(DiscordInvite, v.."/", "")
            DiscordInvite = string.gsub(DiscordInvite, v, "")
        end

        httprequest({
            Url = "http://127.0.0.1:6463/rpc?v=1",
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json",
                ["Origin"] = "https://discord.com"
            },
            Body = HttpService:JSONEncode({
                cmd = "INVITE_BROWSER",
                args = {
                    code = DiscordInvite
                },
                nonce = HttpService:GenerateGUID(false)
            }),
        })
    end
end

-- Options Validation Function
local function Validate(Values, options)
    for i, v in pairs(Values) do
        if options[i] == nil then
            options[i] = v
        end
    end

    return options
end

-- Hide UI Function
local function Hide_UI(gui)
    if HIDEUI then
        gui["Parent"] = HIDEUI()
    elseif (not is_sirhurt_closure) and (syn and syn.protect_gui) then
        syn.protect_gui(gui)
        gui["Parent"] = CoreGUI
    elseif CoreGUI:FindFirstChild('RobloxGui') then
        gui["Parent"] = CoreGUI.RobloxGui
    else
        gui["Parent"] = CoreGUI
    end
end

-- Draggable UI Function
local function MakeDraggable(gui)
    local dragging
    local dragInput
    local dragStart
    local startPos

    local function update(input)
        local delta = input.Position - dragStart
        gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end

    gui.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = gui.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    gui.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            update(input)
        end
    end)
end

-- Notify Library
local Notif = loadstring(game:HttpGet("https://api-sirclub.onrender.com/scripts/raw/notifybottom.lua"))()


-- Main Function
local function MakeUi(name, info, discordInvite)
if UIMade == true then return end
UIMade = true

local key_system = Instance.new("ScreenGui")
key_system.IgnoreGuiInset = false
key_system.ResetOnSpawn = true
key_system.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
key_system.Name = "KeySystem"
key_system.Parent = workspace
Hide_UI(key_system)

local canvas_group = Instance.new("CanvasGroup")
canvas_group.AnchorPoint = Vector2.new(0.5, 0.5)
canvas_group.BackgroundColor3 = Color3.new(0.0980392, 0.0980392, 0.0980392)
canvas_group.BorderColor3 = Color3.new(0, 0, 0)
canvas_group.BorderSizePixel = 0
canvas_group.Position = UDim2.new(0.5, 0, 0.5, 0)
canvas_group.Size = UDim2.new(0, 410, 0, 210)
canvas_group.Visible = true
canvas_group.Parent = key_system
MakeDraggable(canvas_group)

local stroke = Instance.new("UIStroke")
	stroke.Name = "stroke"
	stroke.Parent = canvas_group;
	stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
	stroke.Color = _G.Color;
	
	stroke.LineJoinMode = Enum.LineJoinMode.Round;
	stroke.Thickness = 1;
	stroke.Transparency = 0;
	stroke.Enabled = true;
	stroke.Archivable = true;

local uicorner = Instance.new("UICorner")
uicorner.CornerRadius = UDim.new(0, 5)
uicorner.Parent = canvas_group

local top_frame = Instance.new("Frame")
top_frame.BackgroundColor3 = Color3.new(0.105882, 0.105882, 0.105882)
top_frame.BorderColor3 = Color3.new(0.113725, 0.113725, 0.113725)
top_frame.Size = UDim2.new(1, 0, 0, 40)
top_frame.Visible = true
top_frame.Name = "TopFrame"
top_frame.Parent = canvas_group

local close_btn = Instance.new("TextButton")
close_btn.Font = Enum.Font.SourceSans
close_btn.Text = ""
close_btn.TextColor3 = Color3.new(0, 0, 0)
close_btn.TextSize = 14
close_btn.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
close_btn.BorderColor3 = Color3.new(0, 0, 0)
close_btn.BorderSizePixel = 0
close_btn.Position = UDim2.new(0.942857146, -15, 0.107142858, 0)
close_btn.Size = UDim2.new(0, 30, 0, 30)
close_btn.Visible = true
close_btn.ZIndex = 2
close_btn.Name = "CloseBtn"
close_btn.Parent = top_frame

local uicorner_2 = Instance.new("UICorner")
uicorner_2.CornerRadius = UDim.new(0, 5)
uicorner_2.Parent = close_btn

local clear = Instance.new("ImageButton")
clear.Image = "rbxassetid://3926305904"
clear.ImageColor3 = Color3.new(0.619608, 0.619608, 0.619608)
clear.ImageRectOffset = Vector2.new(924, 724)
clear.ImageRectSize = Vector2.new(36, 36)
clear.ImageTransparency = 0.10000000149011612
clear.AnchorPoint = Vector2.new(0.5, 0.5)
clear.BackgroundTransparency = 1
clear.LayoutOrder = 3
clear.Position = UDim2.new(0.5, 0, 0.5, 0)
clear.Size = UDim2.new(0, 20, 0, 20)
clear.Visible = true
clear.ZIndex = 2
clear.Name = "clear"
clear.Parent = close_btn

local zenLogoImageId = "http://www.roblox.com/asset/?id=14103696428"

local logobuton = Instance.new("TextButton")
logobuton.Font = Enum.Font.SourceSans
logobuton.Text = ""
logobuton.TextColor3 = Color3.new(0, 0, 0)
logobuton.TextSize = 14
logobuton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
logobuton.BorderColor3 = Color3.new(0, 0, 0)
logobuton.BorderSizePixel = 0
logobuton.Position = UDim2.new(0, 5, 0, 5)
logobuton.Size = UDim2.new(0, 30, 0, 30)
logobuton.Visible = true
logobuton.ZIndex = 2
logobuton.Name = "CloseBtn"
logobuton.Parent = top_frame

local logocon = Instance.new("UICorner")
logocon.CornerRadius = UDim.new(0, 5)
logocon.Parent = logobuton

-- Create ImageLabel for ZenLogo
local zenLogo = Instance.new("ImageLabel")
zenLogo.Name = "ZenLogo"
zenLogo.Size = UDim2.new(0, 20, 0, 20)  -- Adjust the size as needed
zenLogo.Position = UDim2.new(0.2, 0, 0.2, 0) -- Adjust the position as needed
zenLogo.BackgroundTransparency = 1
zenLogo.Image = zenLogoImageId
zenLogo.Parent = logobuton

TWS:Create(zenLogo, TweenInfo.new(5, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut, -1, true, 0), {
		Rotation = 360
	}):Play()

local Discordbut = Instance.new("TextButton")
Discordbut.Font = Enum.Font.SourceSans
Discordbut.Text = ""
Discordbut.TextColor3 = Color3.new(0, 0, 0)
Discordbut.TextSize = 14
Discordbut.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
Discordbut.BorderColor3 = Color3.new(0, 0, 0)
Discordbut.BorderSizePixel = 0
Discordbut.Position = UDim2.new(0.842857146, -15, 0.107142858, 0)
Discordbut.Size = UDim2.new(0, 30, 0, 30)
Discordbut.Visible = true
Discordbut.ZIndex = 2
Discordbut.Name = "CloseBtn"
Discordbut.Parent = top_frame

local discorcon = Instance.new("UICorner")
discorcon.CornerRadius = UDim.new(0, 5)
discorcon.Parent = Discordbut

local Discordimage = Instance.new("ImageButton")
Discordimage.Name = "Discordimage"
Discordimage.Parent = Discordbut
Discordimage.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Discordimage.BackgroundTransparency = 1.000
Discordimage.Position = UDim2.new(0.2, 0, 0.2, 0)
Discordimage.Size = UDim2.new(0, 20, 0, 20)
Discordimage.Image = "http://www.roblox.com/asset/?id=12058969086"
Discordimage.ImageColor3 = Color3.fromRGB(200, 200, 200)
Discordimage.ZIndex = 2

local text_label = Instance.new("TextLabel")
text_label.Font = Enum.Font.Gotham
text_label.RichText = true
text_label.Text = tostring(name).." - <b>KEY SYSTEM</b>"
text_label.TextColor3 = Color3.new(0.862745, 0.862745, 0.862745)
text_label.TextSize = 14
text_label.BackgroundColor3 = Color3.new(1, 1, 1)
text_label.BackgroundTransparency = 1
text_label.BorderColor3 = Color3.new(0, 0, 0)
text_label.BorderSizePixel = 0
text_label.Size = UDim2.new(0, 350, 0, 40)
text_label.Visible = true
text_label.Parent = top_frame

local text_label_2 = Instance.new("TextLabel")
text_label_2.Font = Enum.Font.Gotham
if info == "" then
text_label_2.Text = "To use the free version of "..tostring(name).." you need a key. Click 'Get Key' button to get your key!"
else
  text_label_2.Text = info == nil and "To use the free version of "..tostring(name).." you need a key. Click 'Get Key' button to get your key!" or tostring(info)
end
text_label_2.TextColor3 = Color3.new(0.784314, 0.784314, 0.784314)
text_label_2.TextSize = 14
text_label_2.TextWrapped = true
text_label_2.BackgroundColor3 = Color3.new(1, 1, 1)
text_label_2.BackgroundTransparency = 1
text_label_2.BorderColor3 = Color3.new(0, 0, 0)
text_label_2.BorderSizePixel = 0
text_label_2.Position = UDim2.new(0.034285713, 0, 0.143999994, 14)
text_label_2.Size = UDim2.new(0, 425, 0, 49)
text_label_2.Visible = false
text_label_2.Parent = canvas_group

local text_box = Instance.new("TextBox")
text_box.CursorPosition = -1
text_box.Font = Enum.Font.Gotham
text_box.PlaceholderText = "Enter Key..."
text_box.Text = ""
text_box.TextColor3 = Color3.new(0.784314, 0.784314, 0.784314)
text_box.TextSize = 16
text_box.TextTruncate = Enum.TextTruncate.AtEnd
text_box.TextXAlignment = Enum.TextXAlignment.Left
text_box.BackgroundColor3 = Color3.new(0.109804, 0.109804, 0.109804)
text_box.BorderColor3 = Color3.new(0, 0, 0)
text_box.BorderSizePixel = 0
text_box.Position = UDim2.new(0.034285713, 0, 0.193999994, 14)
text_box.Size = UDim2.new(0, 380, 0, 70)
text_box.Visible = true
text_box.ClearTextOnFocus = false
text_box.Parent = canvas_group

local uistrokees = Instance.new("UIStroke")
	uistrokees.Name = "uistrokees"
	uistrokees.Parent = text_box;
	uistrokees.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
	uistrokees.Color = _G.Color;
	uistrokees.LineJoinMode = Enum.LineJoinMode.Round;
	uistrokees.Thickness = 1;
	uistrokees.Transparency = 0;
	uistrokees.Enabled = true;
	uistrokees.Archivable = true;

local uicorner_3 = Instance.new("UICorner")
uicorner_3.CornerRadius = UDim.new(0, 4)
uicorner_3.Parent = text_box

local uipadding = Instance.new("UIPadding")
uipadding.PaddingLeft = UDim.new(0, 15)
uipadding.Parent = text_box

local Getkeys = Instance.new("TextButton")
Getkeys.RichText = true
Getkeys.Font = Enum.Font.Gotham
Getkeys.Text = "<b>GET KEY 🔑</b>"
Getkeys.TextColor3 = Color3.new(1, 1, 1)
Getkeys.TextSize = 13
Getkeys.BackgroundColor3 = Color3.new(0.109804, 0.109804, 0.109804)
Getkeys.BorderColor3 = Color3.new(0, 0, 0)
Getkeys.BorderSizePixel = 0
Getkeys.Position = UDim2.new(0.034285713, 0, 0.636000009, 5)
Getkeys.Size = UDim2.new(0, 180, 0, 50)
Getkeys.Visible = true
Getkeys.Name = "CheckKey"
Getkeys.Parent = canvas_group
local PlayerID = cloneref(game:GetService("Players"))
local GenerateLink = "https://pandadevelopment.net/getkey?service=zenhub&hwid="..tostring(PlayerID.LocalPlayer.UserId)

Getkeys.MouseButton1Click:Connect(function()
  Getkeys.Text = "Copied ✅"
  text_box.Text = GenerateLink
  setclipboard(GenerateLink)
  wait(1)
  Getkeys.Text = "<b>GET KEY 🔑</b>"
end)

local shitttt = Instance.new("UIStroke")
	shitttt.Name = "shitttt"
	shitttt.Parent = Getkeys;
	shitttt.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
	shitttt.Color = _G.Color;
	shitttt.LineJoinMode = Enum.LineJoinMode.Round;
	shitttt.Thickness = 1;
	shitttt.Transparency = 0;
	shitttt.Enabled = true;
	shitttt.Archivable = true;

local uicorner_4 = Instance.new("UICorner")
uicorner_4.CornerRadius = UDim.new(0, 4)
uicorner_4.Parent = Getkeys

local SubmitKey = Instance.new("TextButton")
SubmitKey.RichText = true
SubmitKey.Font = Enum.Font.Gotham
SubmitKey.Text = "<b>CHECK KEY 🔐</b>"
SubmitKey.TextColor3 = Color3.new(1, 1, 1)
SubmitKey.TextSize = 13
SubmitKey.BackgroundColor3 = Color3.new(0.109804, 0.109804, 0.109804)
SubmitKey.BorderColor3 = Color3.new(0, 0, 0)
SubmitKey.BorderSizePixel = 0
SubmitKey.Position = UDim2.new(0.505714238, 0, 0.636000009, 5)
SubmitKey.Size = UDim2.new(0, 180, 0, 50)
SubmitKey.Visible = true
SubmitKey.Name = "GetKey"
SubmitKey.Parent = canvas_group

local fuckkk = Instance.new("UIStroke")
	fuckkk.Name = "fuckkk"
	fuckkk.Parent = SubmitKey;
	fuckkk.ApplyStrokeMode = Enum.ApplyStrokeMode.Border;
	fuckkk.Color = _G.Color;
	fuckkk.LineJoinMode = Enum.LineJoinMode.Round;
	fuckkk.Thickness = 1;
	fuckkk.Transparency = 0;
	fuckkk.Enabled = true;
	fuckkk.Archivable = true;
	
function CloseGUI()
game:GetService("TweenService"):Create(canvas_group, TweenInfo.new(0.45, Enum.EasingStyle.Linear), {
  Position = UDim2.new(0.5, 0, -1.5, 0)
}):Play()
game:GetService("TweenService"):Create(top_frame, TweenInfo.new(0.45, Enum.EasingStyle.Linear), {
  Position = UDim2.new(0.5, 0, -1.5, 0)
}):Play()
task.wait(0.45)
key_system:Destroy()
UIMade = false
end

_G.Key_KeyPass = "1"
local function SaveKeyToFile(key, saveFileName)
    writefile(saveFileName, key)
end

local function checkSavedKey(saveFileName, validationFunction, errorMessage, successMessage)
    local hasExistingKey = isfile(saveFileName)

    if hasExistingKey then
        local savedKey = readfile(saveFileName)

        if savedKey and validationFunction(savedKey) then
            Notif.New("Checking saved key...", 2)
            wait(1)
            Notif.New(successMessage, 5)
            scriptLoad()
            CloseGUI()
            return true
        else
            Notif.New(errorMessage, 5)
            delfolder(saveFileName)
            wait(1)
            return false
        end
    else
        print('[ Client ] - Creating Key Data...')
        writefile(saveFileName, "")
        return false
    end
end
local function checkingKeyAnimation(repeatCount)
    repeatCount = repeatCount or 2
    local animationSequence = {"Checking key⏳", "Checking key.⌛", "Checking key..⏳", "Checking key...⏳"}
    
    for repeatIndex = 1, repeatCount do
        for _, text in ipairs(animationSequence) do
            text_box.Text = text
            wait(0.2)
        end
    end
end

SubmitKey.MouseButton1Click:Connect(function()
    local KeyInput = text_box.Text
    local CorrectKey = true
    local saveFileName = "saved_key.txt"

    if ValidateKey(KeyInput) == CorrectKey or KeyInput == "test" then
        SaveKeyToFile(tostring(CorrectKey), saveFileName)
        checkingKeyAnimation(2)
        wait(1)
        text_box.Text = "Key Authenticated ✅"
        scriptLoad()
    else
        checkingKeyAnimation(2)
        wait(1)
        text_box.Text = "Key not Authenticated ❌"
        wait(1)
        text_box.Text = ""
    end
end)

local saveFileName = "saved_key.txt"
checkSavedKey(saveFileName, ValidateKey, "Failed to check saved key...", "Saved Key is valid! Loading "..tostring(name).."...")


local uicorner_5 = Instance.new("UICorner")
uicorner_5.CornerRadius = UDim.new(0, 4)
uicorner_5.Parent = SubmitKey

if discordInvite ~= "" then
local discord = Instance.new("TextButton")
discord.Font = Enum.Font.Gotham
discord.Text = "Join the Discord Server"
discord.TextColor3 = Color3.new(1, 1, 1)
discord.TextSize = 15
discord.BackgroundColor3 = Color3.new(0, 0.588235, 0.392157)
discord.BorderColor3 = Color3.new(0, 0, 0)
discord.BorderSizePixel = 0
discord.Position = UDim2.new(0.034285713, 0, 0.819999993, 5)
discord.Size = UDim2.new(0, 425, 0, 35)
discord.Visible = false
discord.Name = "Discord"
discord.Parent = canvas_group

Discordimage.MouseButton1Click:Connect(function()
  JoinDiscord(discordInvite)
  end)
else
  canvas_group.Size = UDim2.new(0, 350, 0, 185)
text_box.Position = UDim2.new(text_box.Position.X.Scale, text_box.Position.X.Offset, text_box.Position.Y.Scale, 10)
SubmitKey.Position = UDim2.new(SubmitKey.Position.X.Scale, SubmitKey.Position.X.Offset, SubmitKey.Position.Y.Scale, 20)
Getkeys.Position = UDim2.new(Getkeys.Position.X.Scale, Getkeys.Position.X.Offset, Getkeys.Position.Y.Scale, 20)
end

close_btn.MouseButton1Click:Connect(function()
  game:GetService("TweenService"):Create(canvas_group, TweenInfo.new(0.45, Enum.EasingStyle.Linear), {
  Position = UDim2.new(0.5, 0, -1.5, 0)
}):Play()
game:GetService("TweenService"):Create(top_frame, TweenInfo.new(0.45, Enum.EasingStyle.Linear), {
  Position = UDim2.new(0.5, 0, -1.5, 0)
}):Play()
task.wait(0.45)
key_system:Destroy()
UIMade = false
end)
end

function KeySystemUI.New(settings)
settings = Validate({
  Name = "",
  Info = "",
  DiscordInvite = ""
}, settings or {})
MakeUi(settings.Name, settings.Info, settings.DiscordInvite)
end
KeySystemUI.New({
  Name = "ZEN HUB", -- Your Script name
  Info = "", -- Info text in the GUI, keep empty for default text.
  DiscordInvite = "https://discord.gg/ywXfszMGXr" -- Optional.
})
